# Require secure remote add-on transports

Kino will install and contact remote add-ons over HTTPS. Plain HTTP is allowed only for loopback addresses during local development. Version one will manage synced and installed add-ons, support configuration and manual manifest URLs, and ship only official defaults; it will not include a Kino-curated or community add-on directory.
